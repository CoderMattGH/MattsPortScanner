/*
 * Function: validate_mac_add
 * --------------------------
 * Validates the supplied MAC address.
 * 
 * mac_add: The MAC address represented in array format.
 * 
 * return: 1 if the MAC address is valid and 0 if invalid.
 */
unsigned char validate_mac_add(const unsigned char *mac_add);